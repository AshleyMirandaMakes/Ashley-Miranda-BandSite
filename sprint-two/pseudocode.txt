new content - bio page

MOBILE
title
input comment section - form with name -- HTML
                  image
                  text input field
                  comment 
                  comment input field
                  button
                  divider ---- all dividers are the same
        html form should (functionality requirement): -- JS
                  submit using addEventListener
                  prevent the page when submitting a new comment
                  constructs a new comment object
                  pushes new object into an array of comments
                  clears all comments from page
                  re-renders the page with all comments in array
                  clears the input field after submitting new comment
existing comment section - (x3)
                  image placeholder
                  commenter name
                  comment timestamp
                  commenter comment 
                  divider 
          PUT THIS SECTION IN A CONTAINER
TABLET
this container is now wider. image is still the dame width, but everything else is maybe 90%?
DESKTOP
this container is now wider. image is still the dame width, but everything else is maybe 35%?

styling for the comment input field and existing comments similar -
       perhaps I can reuse a card structure for all of them?
function displayComment() takes one comment object as param and displays
  it on the page using DOM-man
no templates should be used. avoid using innerHTML.

----------------------------------------------------------------------------------------------
new content - shows page 

obviously, reuse the nav and footer sections

MOBILE 
you can mostly reuse the hero styling with a few tweaks 
       add a song from soundcloud -- HTML
SHOWS section 
title ------------------------------ HTML
show cards                   (x6) -- JS render
           show date title
           show date
           show venue title
           show venue
           show location title
           show location
           button ------same styling as bio ish
           divider -----same styling as bio ish
TABLET
title ------------------------------ HTML
3 subtitles ---- can I HTML>
6 cards- adjusted to rows - JS
DESKTOP
title ------------------------------ HTML
3 subtitles ---- can I HTML>
6 cards- adjusted to rows - JS
but inside a container


